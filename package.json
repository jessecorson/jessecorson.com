{
    "homepage": "https://github.com/jessecorson/jessecorson.com#readme",
    "author": "Jesse Corson",
    "license": "Apache License 2.0",
    "scripts": {
        "build": "yarn add -D vuepress && which vuepress && vuepress build",
        "sync": "aws s3 sync --delete .vuepress/dist/ s3://www.jessecorson.com",
        "test": "yarn add -D vuepress && which vuepress && vuepress dev"
    },
    "devDependencies": {
        "vuepress": "^1.8.1"
    }
}
